# 复制为根目录 .env，填写敏感配置后执行 docker compose up -d --build
# 不要将 .env 提交到版本库。Compose 会覆盖容器内部的数据库、Redis、Sandbox 地址。

# 对外入口
NGINX_PORT=8088
TZ=Asia/Shanghai

# Postgres
POSTGRES_DB=manus
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-postgres-password

# API 基础配置
ENV=production
LOG_LEVEL=INFO
APP_CONFIG_FILEPATH=config.yaml
SQLALCHEMY_DATABASE_URL=postgresql+asyncpg://postgres:change-this-postgres-password@manus-postgres:5432/manus

# Redis
REDIS_HOST=manus-redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# 腾讯云 COS
COS_SECRET_ID=
COS_SECRET_KEY=
COS_REGION=ap-guangzhou
COS_SCHEME=https
COS_BUCKET=
COS_DOMAIN=

# Sandbox
# 留空 = 每个会话动态创建独立沙箱容器(manus-sandbox-<随机8位>)，空闲60分钟后自毁。
# 一旦填了地址，所有会话将共用该固定沙箱，不再按会话创建(见 DockerSandbox.create)。
SANDBOX_ADDRESS=
SANDBOX_IMAGE=manus-sandbox:latest
SANDBOX_NAME_PREFIX=manus-sandbox
SANDBOX_TTL_MINUTES=60
SANDBOX_NETWORK=manus-network
SANDBOX_CHROME_ARGS=
SANDBOX_HTTPS_PROXY=
SANDBOX_HTTP_PROXY=
SANDBOX_NO_PROXY=

# LLM/MCP 配置写入 api/config.yaml，或按项目配置方式提供。
